[project]
name = "ai-governance"
version = "0.1.0"
description = "AI regulatory compliance testing platform"
authors = [
    {name = "Rhishikumar", email = "rhishikumarayyappan@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.11,<3.12"

# Phase 0 dependencies only. Heavier groups (fairlearn/shap/lime/scikit-learn,
# streamlit, reportlab, garak/ollama) are added at the start of the phase that
# first uses them. See docs/BUILD_PLAN.md.
dependencies = [
    "fastapi>=0.115,<1.0",
    "uvicorn[standard]>=0.30,<1.0",
    "sqlalchemy>=2.0,<3.0",
    "pydantic>=2.7,<3.0",
    "pydantic-settings>=2.3,<3.0",
    # Phase 1, Week 1. Upper bounds keep us on releases that still support
    # Python 3.11 (numpy/scipy/pandas dropped 3.11 in their newest versions).
    "numpy>=1.26,<2.5",
    "scipy>=1.11,<1.18",
    "pandas>=2.1,<3.1",
    "scikit-learn>=1.4,<2.0",
    "fairlearn (>=0.14.0,<0.15.0)",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0,<9.0",
    "httpx>=0.27,<1.0",
]

[tool.poetry]
packages = [
    { include = "governance" },
]

[tool.poetry.group.dev.dependencies]
pytest = ">=8.0,<9.0"
httpx = ">=0.27,<1.0"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
